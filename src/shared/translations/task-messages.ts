import { defineMessage } from "@lingui/macro";

export const TASK_FOOTER_MESSAGES = {
  OK: defineMessage({
    id: "TASK.FOOTER.OK",
  }),
  CANCEL: defineMessage({
    id: "TASK.FOOTER.CANCEL",
  }),
  SUBMIT: defineMessage({
    id: "TASK.FOOTER.SUBMIT",
  }),
};
