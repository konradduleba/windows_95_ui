import { defineMessage } from "@lingui/macro";

export const REGISTER_USER_MESSAGES = {
  TITLE: defineMessage({
    id: "REGISTER_USER.HEADER.TITLE",
  }),
  LOGIN: defineMessage({
    id: "REGISTER_USER.FOOTER.LOGIN",
  }),
};
