import { defineMessage } from "@lingui/macro";

export const MESSAGES = {
  DATE_AND_TIME: defineMessage({
    id: "TASKS.DATE_TIME_PROPERTIES.DATE_TIME_TABS.DATE_AND_TIME",
  }),
  TIME_ZONE: defineMessage({
    id: "TASKS.DATE_TIME_PROPERTIES.DATE_TIME_TABS.TIME_ZONE",
  }),
};
