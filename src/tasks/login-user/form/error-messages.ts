import { defineMessage } from "@lingui/macro";

export const ERROR_MESSAGES = {
  USERNAME: {
    REQUIRED: defineMessage({
      id: "LOGIN_USER.ERROR_MESSAGES.USERNAME.REQUIRED",
    }),
  },
  PASSWORD: {
    REQUIRED: defineMessage({
      id: "LOGIN_USER.ERROR_MESSAGES.PASSWORD.REQUIRED",
    }),
  },
};
