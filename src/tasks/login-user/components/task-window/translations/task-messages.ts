import { defineMessage } from "@lingui/macro";

export const LOGIN_USER_MESSAGES = {
  TITLE: defineMessage({
    id: "LOGIN_USER.HEADER.TITLE",
  }),
  REGISTER: defineMessage({
    id: "LOGIN_USER.FOOTER.REGISTER",
  }),
};
