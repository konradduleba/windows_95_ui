import { defineMessage } from "@lingui/macro";

export const SUBMIT_MESSAGES = {
  ERROR_TITLE: defineMessage({
    id: "LOGIN_USER.RESPONSE.ERROR_TITLE",
  }),
  INVALID_USERNAME_OR_PASSWORD: defineMessage({
    id: "LOGIN_USER.RESPONSE.INVALID_USERNAME_OR_PASSWORD",
  }),
};
